# Core Libraries for RAG Model
openai==0.27.10          # For OpenAI API to access GPT models
pinecone-client==2.2.2   # Pinecone for vector database and similarity search

# Embedding Models and NLP Libraries
transformers==4.31.0     # For managing language models and embeddings
torch==2.0.1             # PyTorch, a deep learning framework required by transformers

# Utility Libraries
numpy==1.24.3            # For numerical operations
scikit-learn==1.3.0      # For vector transformations and metrics
pandas==2.0.3            # Data manipulation and analysis

# Optional: Flask for API Creation (if building a Flask-based QA Bot API)
flask==2.3.2 
